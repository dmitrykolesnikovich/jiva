artifact "jiva:parser-desktop"

apply plugin: "antlr"

dependencies {
    antlr "org.antlr:antlr4:4.5"
}

generateGrammarSource {
    maxHeapSize = "64m"
    arguments += ["-visitor", "-long-messages", "-package", "jiva.parser"]
}
